import {styled} from "@mui/material";

const Title = styled("p")({
	fontSize: "1.2rem",
});

const Info = styled("p")({
	color: "#0000008F",
});

const ListTitle = styled("p")({
	listStyle: "none",
	color: "#3D3D3D",
	fontSize: "1.1rem",
	width: "100%"
});

export {Info, Title, ListTitle};